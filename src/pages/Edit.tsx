import styled from "styled-components";
import Header from "../components/Header";

const Container = styled.div``;

const Edit = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Edit;
